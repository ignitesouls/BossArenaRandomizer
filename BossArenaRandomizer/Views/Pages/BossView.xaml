<UserControl x:Class="BossArenaRandomizer.Views.Pages.BossView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,18">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <StackPanel>
        <TextBlock Text="{Binding Title}" FontSize="30" FontWeight="Bold" Foreground="White"/>
        <TextBlock Text="{Binding Subtitle}" Margin="0,8,0,0" FontSize="14" Foreground="#A9A9B3"/>
      </StackPanel>

      <Border Grid.Column="1" Background="#171720" CornerRadius="14" Padding="16">
        <StackPanel>
          <TextBlock Text="{Binding SelectedCount, StringFormat=Selected: {0}}" Foreground="White"/>
          <TextBlock Text="{Binding TotalCount, StringFormat=Total: {0}}" Margin="0,4,0,0" Foreground="#A9A9B3"/>
        </StackPanel>
      </Border>
    </Grid>

    <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,18">
      <Border Background="#171720" CornerRadius="16" Padding="16" Margin="0,0,12,0">
        <StackPanel>
          <TextBlock Text="Search" Foreground="#A9A9B3"/>
          <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,0"/>
        </StackPanel>
      </Border>

      <Border Background="#171720" CornerRadius="16" Padding="16" Margin="0,0,12,0">
        <StackPanel>
          <TextBlock Text="Boss Preset" Foreground="#A9A9B3"/>
          <ComboBox ItemsSource="{Binding BossPresets}"
                    SelectedItem="{Binding SelectedBossPreset, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Margin="0,10,0,0"/>
          <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Load" Command="{Binding LoadPresetCommand}" Width="80" Margin="0,0,8,0"/>
            <Button Content="Refresh" Command="{Binding RefreshPresetsCommand}" Width="80"/>
          </StackPanel>
        </StackPanel>
      </Border>

      <Border Background="#171720" CornerRadius="16" Padding="16" Margin="0,0,12,0">
        <StackPanel>
          <TextBlock Text="Bulk Actions" Foreground="#A9A9B3"/>
          <Button Content="Reset All" Command="{Binding ResetAllCommand}" Margin="0,10,0,0"/>
          <Button Content="Clear All" Command="{Binding ClearAllCommand}" Margin="0,8,0,0"/>
        </StackPanel>
      </Border>

      <Border Background="#171720" CornerRadius="16" Padding="16">
        <StackPanel>
          <TextBlock Text="Game Split" Foreground="#A9A9B3"/>
          <Button Content="Base Game Only" Command="{Binding SelectBaseGameCommand}" Margin="0,10,0,0"/>
          <Button Content="DLC Only" Command="{Binding SelectDlcCommand}" Margin="0,8,0,0"/>
          <Button Content="Save Preset" Command="{Binding SavePresetCommand}" Margin="0,8,0,0"/>
        </StackPanel>
      </Border>
    </UniformGrid>

    <Border Grid.Row="2" Background="#171720" CornerRadius="16" Padding="16">
      <DataGrid ItemsSource="{Binding BossSelectionsView}"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                HeadersVisibility="Column"
                RowHeaderWidth="0"
                IsReadOnly="False">
        <DataGrid.Columns>
          <DataGridCheckBoxColumn Header="Use" Binding="{Binding IsSelected, Mode=TwoWay}" Width="70"/>
          <DataGridTextColumn Header="Boss" Binding="{Binding Name}" Width="*"/>
          <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="180"/>
          <DataGridTextColumn Header="Region" Binding="{Binding RegionName}" Width="220"/>
        </DataGrid.Columns>
      </DataGrid>
    </Border>
  </Grid>
</UserControl>